server:
  port: 9468
spring:
  application:
    name: drugstore-admin
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
feign:
  okhttp:
    enabled: true
ribbon:
  ConnectTimeout: 3000 #服务请求连接超时时间（毫秒）
  ReadTimeout: 3000 #服务请求处理超时时间（毫秒）
